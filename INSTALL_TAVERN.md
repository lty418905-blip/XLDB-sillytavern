# 安装 XLDB 酒馆插件

适用 Windows x64、Node.js 24.18.1 及以上的 24.x 版本，酒馆与 XLDB 核心在同一台电脑。安装器优先复用兼容的 Node.js，否则将校验后下载便携版本；LanceDB 和 TypeScript 安装在本目录 `.local/`。不修改全局 Python 环境。

1. 把完整分发目录解压到 D 盘或其他有写入权限的位置，运行根目录 `START-XLDB.cmd`。首次运行会从固定版本资源下载并校验 AgentJev 模型（约 2.4 GB）及便携运行时，然后安装本地依赖、启动核心并打开酒馆页面；需要联网并留出足够磁盘空间。后续启动复用已校验的资源。默认酒馆地址是 `http://localhost:8000`；自定义地址可运行 `START-XLDB.cmd -TavernOrigin http://localhost:端口`。需要修改核心端口时传入 `-Port`。
2. 在酒馆助手的脚本导入界面选择 `XLDB-酒馆.json`，选定脚本库并启用。助手导入的新脚本可能默认关闭，请确认开关已打开。也可新建脚本，粘贴 `XLDB-酒馆.js` 的全部内容。两种方式只启用一种，不要再启用旧的 XLDB 直连脚本。
3. 启动器打开的酒馆页面会完成一次性配对。左下角 **XLDB 连接** 可查看连接状态及重新连接入口。然后在 XLDB 工作台配置一套后台文本模型 API 地址、密钥与模型；需要时可逐阶段覆盖，embedding 和 reranker 分别可选。聊天正文模型仍在酒馆中配置。

再次打开酒馆时，核心仍在运行会自动重连；电脑重启后重新运行 `START-XLDB.cmd`。启动器沿用此安装上次成功的端口、数据目录和私有配置目录。普通暂时失败会在后续正常生成中有限重试；更换聊天、角色卡或世界书导致来源或范围变化时，按工作台提示核对并恢复。已接受正文不会因为重试自动再次插入聊天。

`tools/stop.ps1` 只停止属于本安装记录的核心。数据库默认位于 `.local/data`；访问令牌、模型配置与配对资料默认位于安装目录旁的私有目录。不要把这两个目录提交到源码仓库或分享。数据库备份与聊天恢复见 [恢复说明](RECOVERY.md)。

核心和浏览器不能互相代替：浏览器脚本不能自行启动本机程序。安装本身只证明依赖与核心可用，仍需在真实酒馆中检查角色卡、世界书、模型与完整生成流程。
