# Tavern 数据与连接恢复

连接暂时中断时，保留当前聊天和连接资料；重新运行 `START-XLDB.cmd`，从原酒馆地址打开页面，插件会尝试重连。认证或配对资料失效时，用启动器重新打开酒馆完成配对，原数据库和模型配置不会因重新配对而删除。

普通后台阶段失败会在界面列出未完成环节；后续正常生成按已有重试策略处理。原生聊天的同步暂时失败时，下一次正常生成会先重新同步已接受正文，再组装受限上下文。当前已停止的一次正文不会自动重发。若界面提示角色卡、世界书、聊天范围、未确认候选或权威版本冲突，不要强行续写；按工作台 **资料与恢复** 预览来源变化或核心正文，并核对后确认。

数据库备份工具为 `node tools/backup.mjs create 数据库路径 [输出目录]`。恢复命令为 `node tools/backup.mjs restore 备份目录 数据库路径`；恢复前先运行 `tools/stop.ps1` 停止本安装核心，并在确认备份及目标路径后执行。灾难恢复到新路径使用 `node tools/backup.mjs recover 备份目录 新数据库路径`。数据库、备份及私有配置不包含在源码分发包内。

不要手动删除聊天中的 `xldbScene` 元数据或将一个世界的受管正文改绑到另一个世界。旧消息需要恢复时，工作台会显示目标世界、会话、分支、核心修订与拟改动的正文，确认后才写回酒馆。
